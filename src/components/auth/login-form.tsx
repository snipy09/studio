
"use client";

// This component is no longer used as the login functionality is simplified
// and handled directly in `src/app/(auth)/login/page.tsx` for the dummy login.
// Keeping the file for structure but it can be considered for removal
// if no other login methods are planned.

import * as React from "react";

export function LoginForm() {
  // Content removed as login is handled directly on the page for dummy login
  return null; 
}
